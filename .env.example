INDECX_COMPANY_KEY=replace-with-your-company-key
# stdio (default) or http
MCP_TRANSPORT=stdio
PORT=3000
# Required when binding HTTP outside loopback:
# HOST=0.0.0.0
# MCP_BEARER_TOKEN=use-a-long-random-secret
